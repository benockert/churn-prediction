import './Loading.css';

export const Loading = () => {
    return (
        <div className="loader">
            <span className="bar"></span>
            <span className="bar"></span>
            <span className="bar"></span>
        </div>
    );
}